<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ainannan.sys.dict.mapper.DictMapper" >
    <resultMap id="BaseResultMap" type="cn.ainannan.sys.dict.bean.Dict" >
        <id column="id" property="id"/>
        <result column="label" property="label"/>
        <result column="value" property="value"/>
        <result column="name" property="name"/>
        <result column="sort" property="sort"/>
        <result column="create_user" property="createUser.id"/>
        <result column="create_date" property="createDate"/>
        <result column="update_user" property="updateUser.id"/>
        <result column="update_date" property="updateDate"/>
        <result column="del_flag" property="delFlag"/>
    </resultMap>

    <sql id="Base_Column_List" >
        a.id as id, a.name as name, a.label as label, a.value as value, a.sort as sort, 
        a.create_user as createUser.id, a.create_date as createDate, a.update_user as updateUser.id, 
        a.update_date as updateDate, a.del_flag as delFlag
    </sql>

    <select id="findList" resultMap="BaseResultMap">
        SELECT 
       	<include refid="Base_Column_List" />
        FROM sys_dict a
    </select>

</mapper>